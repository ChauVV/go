// import { takeLatest } from 'redux-saga/effects'
// import key from '../lib/constants'
// import { fetchBalance } from './sagaFetchActions'

export default function * rootSaga () {
  // yield takeLatest(key.GET_BALANCE, fetchBalance)
}
